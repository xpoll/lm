<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="Article">

    <resultMap id="ArticleMap" type="Article">
        <id column="id" property="id"/>
        <result column="owner_id" property="ownerId"/>
        <result column="owner_name" property="ownerName"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="title" property="title"/>
        <result column="number" property="number"/>
        <result column="description" property="description"/>
        <result column="extra_json" property="extraJson"/>
        <result column="tags_json" property="tagsJson"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        parana_articles
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        owner_id, owner_name, `type`, `status`, title, `number`, description,extra_json, tags_json, content, created_at, updated_at
    </sql>

    <sql id="vals">
        #{ownerId}, #{ownerName}, #{type}, #{status}, #{title}, #{number}, #{description},#{extraJson}, #{tagsJson}, #{content}, now(), now()
    </sql>

    <insert id="create" parameterType="Article" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <sql id="criteria">
        1 = 1
        <if test="type != null">AND `type` = #{type}</if>
        <!--<if test="status != null">AND status = #{status}</if>-->
        <choose>
            <when test="status != null and status != ''">
                AND status = #{status}
            </when>
            <otherwise>
                AND status != -2
            </otherwise>
        </choose>
        <if test="ownerId != null">AND owner_id = #{ownerId}</if>
        <if test="title != null">AND title = #{title}</if>
        <if test="number != null">AND `number` = #{number}</if>
        <if test="description != null">AND description = #{description}</if>
        <if test="tagsJson != null">AND tags_json = #{tagsJson}</if>
        <if test="beginAt != null">AND <![CDATA[created_at >= #{beginAt}]]> </if>
        <if test="endAt != null">AND <![CDATA[created_at < #{endAt}]]> </if>
    </sql>

    <update id="update">
        UPDATE <include refid="tb"/>
        SET
        <if test="type != null"> `type` = #{type},</if>
        <if test="status != null"> status = #{status},</if>
        <if test="title != null"> title = #{title},</if>
        <if test="number != null"> `number` = #{number},</if>
        <if test="description != null"> description = #{description},</if>
        <if test="extraJson != null"> extra_json = #{extraJson},</if>
        <if test="tagsJson != null"> tags_json = #{tagsJson},</if>
        <if test="content != null"> content = #{content},</if>
        updated_at = now()
        where id = #{id}
    </update>

    <update id="setStatus" parameterType="map">
        UPDATE <include refid="tb"/>
        SET status = #{status}
        where id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        delete from
        <include refid="tb"/>
        where id = #{id}
    </delete>

    <select id="findById" parameterType="long" resultMap="ArticleMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="findByType" parameterType="int" resultMap="ArticleMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE status = 1 AND `type` = #{type}
    </select>

    <select id="paging" parameterType="map" resultMap="ArticleMap">
        SELECT id,
        <include refid="cols_exclude_id"/>
        FROM
        <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
        order by id desc LIMIT #{offset}, #{limit}
    </select>

    <select id="count" parameterType="map" resultType="long">
        SELECT count(1)
        FROM
        <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>

    <select id="list" parameterType="Article" resultMap="ArticleMap">
        SELECT id,
        <include refid="cols_exclude_id"/>
        FROM
        <include refid="tb"/>
        <where>
            <include refid="criteria"/>
        </where>
    </select>
</mapper>
