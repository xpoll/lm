<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2013 杭州端点网络科技有限公司
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="OwnerPayChannel">

    <resultMap id="OwnerPayChannelMap" type="OwnerPayChannel">
        <id property="id" column="id"/>
        <result property="ownerId" column="owner_id"/>
        <result property="ownerName" column="owner_name"/>
        <result property="type" column="type"/>
        <result property="channel" column="channel"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>


    <sql id="table">
        parana_owner_pay_channels
    </sql>

    <sql id="columns">
        owner_id, owner_name,`type`, channel,created_at, updated_at
    </sql>

    <insert id="create" parameterType="OwnerPayChannel" useGeneratedKeys="true" keyProperty="id">
        insert into <include refid="table"/> (<include refid="columns"/>)
        values
        (
            #{ownerId}, #{ownerName}, #{type}, #{channel},now(), now()
        )
    </insert>

    <select id="load" parameterType="long" resultMap="OwnerPayChannelMap">
        select id,
        <include refid="columns"/>
        from <include refid="table"/>
        where id = #{id}
    </select>

    <update id="update" parameterType="map">
        UPDATE <include refid="table"/>
        <set>
            <if test="channel != null">
                channel = #{channel},
            </if>
            updated_at = now(),
        </set>
        WHERE id=#{id}
    </update>

    <select id="findByOwnerIdAndType" parameterType="map" resultMap="OwnerPayChannelMap">
        select id,
        <include refid="columns"/>
        from <include refid="table"/> where `type` = #{type} AND owner_id = #{ownerId}
    </select>


    <sql id="queryCondition">
        <where>
            <if test="type!=null">`type`=#{type}</if>
            <if test="ownerId!=null">and owner_id=#{ownerId}</if>
            <if test="startAt!=null">and created_at &gt; #{startAt}</if>
            <if test="endAt!=null">and created_at &lt; #{endAt}</if>
        </where>
    </sql>
    <select id="paging" parameterType="list" resultMap="OwnerPayChannelMap">
        select id, <include refid="columns"/>
        from <include refid="table"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="table"/>
        <include refid="queryCondition"/>
    </select>


    <select id="findById" parameterType="long" resultMap="OwnerPayChannelMap">
        select id,
        <include refid="columns"/>
        from <include refid="table"/>
        WHERE id =#{id}
    </select>

</mapper>
