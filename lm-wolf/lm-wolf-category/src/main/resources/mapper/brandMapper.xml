<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016. 杭州端点网络科技有限公司.  All rights reserved.
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="Brand">
    <resultMap id="BrandMap" type="Brand">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="en_name" property="enName"/>
        <result column="en_cap" property="enCap"/>
        <result column="logo" property="logo"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        parana_brands
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        name, en_name, en_cap, logo, description, `status`, created_at, updated_at
    </sql>

    <sql id="vals">
        #{name}, #{enName}, #{enCap}, #{logo}, #{description}, #{status}, now(), now()
    </sql>


    <insert id="create" parameterType="Brand" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <update id="update" parameterType="Brand">
        UPDATE <include refid="tb"/>
        <set>
            <if test="name != null"> name = #{name}, </if>
            <if test="enName != null"> en_name = #{enName}, </if>
            <if test="enCap != null"> en_cap = #{enCap}, </if>
            <if test="logo != null"> logo = #{logo}, </if>
            <if test="description != null"> description = #{description}, </if>
            <if test="status != null"> `status` = #{status}, </if>
            updated_at=now()
        </set>
        WHERE id=#{id}
    </update>

    <update id="updateStatus" parameterType="map">
        UPDATE <include refid="tb"/> set status=#{status}, updated_at=now() where id=#{id}
    </update>

    <select id="findById" parameterType="long" resultMap="BrandMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id} LIMIT 1
    </select>

    <select id="findByIds" parameterType="list" resultMap="BrandMap">
        SELECT id,
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE id IN
        <foreach collection="list" separator="," open="("
                 close=")" item="id">
            #{id}
        </foreach>
    </select>

    <select id="findByName" parameterType="string" resultMap="BrandMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE name = #{name}
    </select>

    <select id="findByEnName" parameterType="string" resultMap="BrandMap">
        SELECT <include refid="cols_all"/>
        FROM <include refid="tb"/>
        WHERE en_name = #{enName}
    </select>

    <select id="findByNamePrefix" parameterType="map" resultMap="BrandMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE
             `name` LIKE CONCAT(#{name}, '%')
              AND status=1
        LIMIT #{limit}
    </select>

    <select id="paging" parameterType="map" resultMap="BrandMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        <where>
            <if test="namePrefix!=null">`name` LIKE CONCAT(#{name}, '%')</if>
        </where>
        LIMIT #{offset},#{limit}
    </select>

    <select id="count"  parameterType="map" resultType="long">
        SELECT count(1)
        FROM
        <include refid="tb"/>
        <where>
            <if test="namePrefix!=null">`name` LIKE CONCAT(#{name}, '%')</if>
        </where>
    </select>
</mapper>