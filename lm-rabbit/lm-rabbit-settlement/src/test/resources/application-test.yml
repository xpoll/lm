spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=MySQL
    schema: classpath:/h2/galaxy-settlement.sql

mybatis:
  mapperLocations:  classpath*:mapper/*Mapper.xml
  typeAliasesPackage: io.terminus.galaxy.settlement.model,io.terminus.parana.settlement.model
